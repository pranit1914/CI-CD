name: Env Demo
on:
  push:
  
env:
  APP_ENV: "dev"

jobs:
  env_demo:
  env:
     APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: Connecting to env
        env:
          APP_ENV: "prod"
        run:  echo "Connecting application to $APP_ENV"
